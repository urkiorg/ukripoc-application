import React from "react";
import { storiesOf } from "@storybook/react";
import { AuthWrapper } from "./index";

storiesOf("Components|AuthWrapper", module).add("Default", () => (
    <AuthWrapper  />
));
