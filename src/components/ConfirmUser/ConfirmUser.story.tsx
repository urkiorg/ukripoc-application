import React from "react";
import { storiesOf } from "@storybook/react";
import { ConfirmUser } from "./index";

storiesOf("Components|ConfirmUser", module).add("Default", () => (
    <ConfirmUser  />
));
